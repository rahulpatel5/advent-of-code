[project]
name = "advent-of-code"
version = "0.1.0"
description = "uv install required python packages"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cvxpy>=1.7.5",
    "numpy>=2.4.0",
    "pyscipopt>=6.0.0",
]
